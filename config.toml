baseurl = "https://x-quant.com.cn"
title = "x-quant"
languageCode = "zh-CN"
theme = "mainroad"
disqusShortname = "" # Enable comments by entering your Disqus shortname
googleAnalytics = "" # Enable Google Analytics by entering your tracking id

[pagination]
  pagerSize = 10 # Number of posts per page

[params]
  description = "x-quant's Personal blog about everything" # Description of your site
  opengraph = true
  twitter_cards = false
  readmore = false # Show "Read more" button in list if true
  authorbox = true
  pager = true
  post_meta = ["date", "categories"] # Order of post meta information
  mainSections = ["post", "docs"]
  ignoreLogs = ["warning-goldmark-raw-html"] 

[params.logo]
  subtitle = "专业的又不沉默的量化投资知识网站" # Logo subtitle

[params.sidebar]
  home = "right"   # Configure layout for home page
  list = "right"   # Configure layout for list pages
  single = "right" # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["search", "recent", "categories", "taglist"]

[params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  tags_counter = false # Enable counter for each tag in "Tags" widget (disabled by default)

# 修复作者配置（替换已弃用的.Site.Author）
[params.author]
  name = "r-quant"
  bio = "r-quant's true identity is unknown. Maybe he is a successful blogger or writer. Nobody knows it."
  avatar = "img/avatar.png"